const base_url = "https://openholidaysapi.org";

export const languageIsoCode = "EN";

export const api_routes = {
	countries: `${base_url}/Countries`,
	holidaysByCountry: `${base_url}/PublicHolidays`,
};
